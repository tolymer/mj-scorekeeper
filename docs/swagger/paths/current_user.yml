'current_user':
  get:
    tags:
      - users
    security:
      - Bearer: []
    responses:
      '200':
        description: Returns a single user
        schema:
          $ref: '#definitions/User'
